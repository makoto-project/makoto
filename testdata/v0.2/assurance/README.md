# Assurance conformance fixtures

Each file under `negative/` identifies one deterministic mutation and the diagnostic that must
prevent a track level or verified property. The test suite applies these mutations to the complete
v0.2 demo evidence. There is one negative fixture for every defined Origin and Transform level
and every verified property.
