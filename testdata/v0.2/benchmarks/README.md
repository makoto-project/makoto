# Benchmark fixture identity

The release benchmark uses this directory path as a stable fixture label. The
end-to-end bundle benchmark will replace this lightweight crypto/canonicalization
probe before the v0.2 release candidate is tagged.
